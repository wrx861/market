# 🔄 Инструкция по автообновлению Market Auto Parts

## Быстрый старт

После того как вы сохранили изменения в GitHub, обновите приложение на сервере одной командой:

```bash
cd /root/market-auto-parts  # или ваша директория
bash deployment/update.sh
```

## Что делает скрипт обновления

Скрипт `update.sh` автоматически выполняет все необходимые шаги:

1. ✅ **Проверяет наличие обновлений** на GitHub
2. ✅ **Сохраняет локальные изменения** (если есть)
3. ✅ **Загружает новый код** с GitHub (`git pull`)
4. ✅ **Останавливает старые контейнеры**
5. ✅ **Пересобирает Docker образы** с новым кодом
6. ✅ **Запускает обновленные сервисы**
7. ✅ **Показывает статус** контейнеров

## Пошаговая инструкция

### Шаг 1: Сохраните изменения в GitHub

На вашей локальной машине или в Emergent:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

### Шаг 2: Подключитесь к серверу

```bash
ssh root@ваш-сервер.ru
```

### Шаг 3: Запустите обновление

```bash
cd /root/market-auto-parts
bash deployment/update.sh
```

### Шаг 4: Проверьте работу

После обновления скрипт автоматически покажет статус контейнеров. Дополнительно можете проверить:

```bash
# Просмотр логов backend
docker-compose logs -f backend

# Просмотр логов frontend
docker-compose logs -f frontend

# Статус всех контейнеров
docker-compose ps

# Проверка работы приложения
curl https://ваш-домен.ru
```

## Создание удобного alias

Для быстрого доступа к команде обновления:

```bash
echo 'alias update-app="cd /root/market-auto-parts && bash deployment/update.sh"' >> ~/.bashrc
source ~/.bashrc
```

Теперь можно обновлять просто командой:

```bash
update-app
```

## Частые вопросы

### ❓ Что если у меня есть локальные изменения на сервере?

Скрипт автоматически сохранит их с помощью `git stash` и попытается восстановить после обновления. Если возникнут конфликты, вы получите уведомление.

### ❓ Как откатить обновление?

```bash
cd /root/market-auto-parts
git log --oneline  # посмотреть историю
git reset --hard COMMIT_HASH  # откатиться на нужный коммит
bash deployment/update.sh  # применить
```

### ❓ Обновятся ли зависимости (npm, pip)?

Да! Скрипт пересобирает Docker образы с флагом `--no-cache`, что заставляет переустановить все зависимости из `package.json` и `requirements.txt`.

### ❓ Что если обновление не сработало?

Проверьте логи:

```bash
docker-compose logs backend
docker-compose logs frontend
```

Попробуйте пересобрать вручную:

```bash
cd /root/market-auto-parts/deployment
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### ❓ Нужно ли пересоздавать .env файлы?

Нет! Скрипт обновляет только код, все файлы конфигурации (.env) остаются без изменений.

### ❓ Произойдет ли downtime?

Да, минимальный (около 30-60 секунд) пока контейнеры пересобираются и перезапускаются.

## Ручные команды управления

Если нужно управлять сервисами вручную:

```bash
cd /root/market-auto-parts/deployment

# Остановить все
docker-compose down

# Пересобрать образы
docker-compose build --no-cache

# Запустить
docker-compose up -d

# Перезапустить отдельный сервис
docker-compose restart backend
docker-compose restart frontend

# Просмотр логов в реальном времени
docker-compose logs -f

# Статус контейнеров
docker-compose ps

# Зайти внутрь контейнера
docker-compose exec backend bash
docker-compose exec frontend bash
```

## Мониторинг после обновления

После каждого обновления рекомендуется:

1. ✅ Проверить что приложение открывается в браузере
2. ✅ Проверить основные функции (поиск запчастей)
3. ✅ Проверить логи на ошибки
4. ✅ Проверить что Telegram бот работает (если используется)

## Автоматизация обновлений (опционально)

Можно настроить автоматическое обновление по расписанию:

```bash
# Создайте cron задачу
crontab -e

# Добавьте строку (обновление каждую ночь в 3:00)
0 3 * * * cd /root/market-auto-parts && bash deployment/update.sh >> /var/log/market-update.log 2>&1
```

## Поддержка

Если возникли проблемы с обновлением:

1. Проверьте что у вас есть доступ к GitHub репозиторию
2. Проверьте что Docker запущен: `systemctl status docker`
3. Проверьте свободное место: `df -h`
4. Проверьте логи: `docker-compose logs`

---

**Важно:** Всегда делайте резервную копию базы данных перед крупными обновлениями!

```bash
# Резервная копия MongoDB
docker-compose exec mongodb mongodump --out /backup
docker cp market-auto-parts-mongodb-1:/backup ./mongodb-backup-$(date +%Y%m%d)
```

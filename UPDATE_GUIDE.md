# 🔄 Руководство по обновлению Market Auto Parts

## Что было исправлено в этом обновлении

### 1. ✅ Удалена кнопка "Made with Emergent"
- Кнопка больше не отображается в нижней части приложения
- Файл: `frontend/public/index.html`

### 2. ✅ Исправлен точный поиск по артикулу в Autotrade
- Добавлен параметр `strict=1` для точного поиска
- Добавлена дополнительная фильтрация на стороне клиента
- Теперь показываются только точные совпадения по артикулу
- Убраны случайные совпадения по буквам

**Изменения:**
- `cross=False` по умолчанию (аналоги отключены)
- `replace=False` по умолчанию (замены отключены)
- `strict=True` по умолчанию (только точное совпадение)
- Дополнительная нормализация и фильтрация артикулов

Файл: `backend/autotrade_client.py`

### 3. ✅ Добавлен Telegram Bot
- Бот теперь запускается автоматически вместе с backend
- Отвечает на команду `/start`
- Поддержка команд: `/start`, `/help`, `/shop`
- Автоматическое определение VIN номеров

Файлы:
- `backend/telegram_bot.py` - код бота
- `backend/start.sh` - скрипт запуска
- `deployment/backend.Dockerfile` - обновлен для запуска бота

### 4. ✅ Улучшены скрипты развертывания
- Добавлен скрипт обновления без полной переустановки
- Сохранение `.env` файлов при обновлении

---

## 🚀 Как обновить приложение на сервере

### Способ 1: Через Git (Рекомендуется)

```bash
# На сервере выполните:
cd /opt/market-auto-parts

# Скачайте скрипт обновления
wget https://raw.githubusercontent.com/wrx861/market/main/deployment/update-on-server.sh
chmod +x update-on-server.sh

# Запустите обновление
sudo bash update-on-server.sh
```

### Способ 2: Вручную

```bash
# 1. Перейдите в директорию проекта
cd /opt/market-auto-parts

# 2. Сохраните .env файлы
cp backend/.env /tmp/backend.env.backup
cp frontend/.env /tmp/frontend.env.backup

# 3. Получите последние изменения
git fetch origin
git reset --hard origin/main

# 4. Восстановите .env файлы
cp /tmp/backend.env.backup backend/.env
cp /tmp/frontend.env.backup frontend/.env

# 5. Пересоберите контейнеры
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 6. Проверьте статус
docker-compose ps
docker-compose logs -f backend
```

---

## 🧪 Проверка обновлений

### 1. Проверка frontend (кнопка удалена)

```bash
# Откройте приложение в браузере
https://miniapp.shopmarketbot.ru

# Прокрутите вниз - кнопки "Made with Emergent" не должно быть
```

### 2. Проверка Autotrade (точный поиск)

```bash
# Проверьте логи backend
docker-compose logs backend | grep "strict=True"

# Должны увидеть строки вида:
# Searching Autotrade for article: 12345, strict=True, cross=False, replace=False
```

**Тест в приложении:**
1. Откройте поиск по артикулу
2. Введите артикул, например: `90919-02252`
3. Должны показаться только точные совпадения
4. Не должно быть результатов типа `90919-02253` или `90919-02251`

### 3. Проверка Telegram Bot

```bash
# Проверьте что бот запущен
docker-compose logs backend | grep "Starting Telegram Bot"
docker-compose logs backend | grep "Bot is running"

# Должны увидеть:
# Starting Telegram Bot...
# Bot is running...
```

**Тест в Telegram:**
1. Найдите своего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением
4. Должна появиться кнопка "🛒 Открыть магазин"

### 4. Общая проверка работоспособности

```bash
# Проверьте статус всех контейнеров
docker-compose ps

# Все 4 должны быть "Up":
# - market-backend
# - market-frontend
# - market-mongodb
# - market-nginx

# Проверьте что нет ошибок
docker-compose logs --tail=50 backend
docker-compose logs --tail=50 frontend

# Проверьте доступность
curl https://miniapp.shopmarketbot.ru
curl https://miniapp.shopmarketbot.ru/api/
```

---

## 🔍 Диагностика проблем

### Telegram Bot не отвечает

```bash
# Проверьте логи
docker-compose logs backend | grep telegram

# Проверьте что токен правильный
docker-compose exec backend env | grep TELEGRAM_BOT_TOKEN

# Перезапустите backend
docker-compose restart backend

# Подождите 10 секунд и проверьте снова
sleep 10
docker-compose logs backend | tail -20
```

### Frontend не обновился (кнопка все еще есть)

```bash
# Очистите кеш браузера
# Или откройте в режиме инкогнито

# Проверьте что frontend пересобрался
docker-compose logs frontend | grep "Configuration complete"

# Пересоберите frontend вручную
docker-compose stop frontend
docker-compose build --no-cache frontend
docker-compose up -d frontend
```

### Autotrade все еще показывает лишние результаты

```bash
# Проверьте код
docker-compose exec backend cat autotrade_client.py | grep "strict"

# Должна быть строка: strict: bool = True

# Проверьте логи поиска
docker-compose logs backend | grep "Searching Autotrade"

# Должно быть: strict=True, cross=False, replace=False
```

---

## 📊 Откат к предыдущей версии

Если что-то пошло не так:

```bash
cd /opt/market-auto-parts

# Вернитесь к предыдущему коммиту
git log --oneline -10
git reset --hard <commit-hash>

# Восстановите .env
cp /tmp/backend.env.backup backend/.env
cp /tmp/frontend.env.backup frontend/.env

# Пересоберите
docker-compose down
docker-compose up --build -d
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs --tail=100`
2. Проверьте статус: `docker-compose ps`
3. Сохраните логи для отладки:
   ```bash
   docker-compose logs > /tmp/market-logs.txt
   ```

---

## ✅ Чек-лист после обновления

- [ ] Все 4 контейнера запущены (backend, frontend, mongodb, nginx)
- [ ] Frontend открывается по HTTPS
- [ ] Кнопка "Made with Emergent" удалена
- [ ] Telegram Bot отвечает на `/start`
- [ ] Поиск по артикулу работает
- [ ] Autotrade показывает только точные совпадения
- [ ] Нет ошибок в логах backend
- [ ] Нет ошибок в логах frontend

---

**Версия:** 2.1  
**Дата:** 2025-11-14
